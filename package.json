{
  "name": "docker-portainer-systray",
  "version": "1.0.0",
  "description": "A small utility shows the status of docker containers on the local environment and opens the portainer page.",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "electron .",
    "package": "electron-packager --overwrite ."
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "electron": "^16.0.6",
    "electron-packager": "^15.4.0"
  }
}
